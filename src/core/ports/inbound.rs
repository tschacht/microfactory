//! Placeholder for future inbound ports (commands/queries flowing into the core).
//! Currently the CLI/server act as inbound adapters that operate on application services,
//! so this module primarily documents the expected structure.

// Once core exposes explicit command/query traits, add them here.
